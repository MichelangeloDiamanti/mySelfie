# mySelfie
mySelfie, be yourselfie

to do list:

IMPORTANT:

1) implementare il sistema di login													- FATTO

2) implementare il sistema dei cookies

3) implementare la funzione di upload selfie										- FATTO

4) implementare il sistema dei followers

5) implementare la funzione "bacheca" dove vengono mostrate le selfie
	5.1) dividere la bacheca in "followed" -> mostra le selfie delle persone che segui
								"global" -> mostra le selfie di tutti (meno la gente che segui)
								
6) implementare la pagina "profilo" dell'utente dove ogniuno può vedere i propi selfie

7) implementare la pagina "settings" dove si possono cambiare i settaggi del proprio account

8) implementare la funzione di ricerca

9) implementare la funzione dei commenti

ALTRO:

1) migliorare il resize/zoom dell'index

2) migliorare il sistema di registrazione, codice di conferma tramite mail

3) Permettere nei commenti di taggare gli utenti e di inserire hashtags, facendoli diventare links alla
	pagina degli hashtags

4) quando si deve mostrare il cuoricino "mi piace" sotto la selfie bisogna controllare se l'utente loggato ha messo mi piace
	il cuore deve essere pieno

MINOR UPDATES:

1) modificare la tabella Comment del DB (text NOT NULL)

2) Mostrare la data nei commenti

